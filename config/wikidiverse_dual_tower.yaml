run_name: WikiDiverse_DualTower
seed: 42
pretrained_model: 'openai/clip-vit-base-patch32'
lr: 5e-5
weight_decay: 0.01
warmup_steps: 1000
version: "dual_tower_v2_improved, 解决MoE和SENet失衡问题"

data:
  num_entity: 132460
  kb_img_folder: 'data/WikiDiverse/kb_image'
  mention_img_folder: 'data/WikiDiverse/mention_images'
  qid2id: 'data/WikiDiverse/qid2id.json'
  entity: 'data/WikiDiverse/kb_entity.json'
  train_file: 'data/WikiDiverse/WikiDiverse_train.json'
  dev_file: 'data/WikiDiverse/WikiDiverse_dev.json'
  test_file: 'data/WikiDiverse/WikiDiverse_test.json'

  batch_size: 64
  num_workers: 8
  text_max_length: 77
  visual_patch_length: 50

  eval_chunk_size: 5000
  eval_batch_size: 16
  embed_update_batch_size: 256

  in_batch_negatives: True
  hard_negatives_per_sample: 3
  use_kg_negatives: False

model:
  text_encoder_dim: 512
  image_encoder_dim: 768
  emd_image_encoder_dim: 512
  unified_dim: 512





  num_attention_heads: 4
  attention_dropout: 0.1

  use_senet: True
  senet_reduction: 16

  text_dropout: 0.3
  image_dropout: 0.3
  fusion_dropout: 0.3

  modal_dropout_prob: 0.3

  temperature: 0.07

  use_memory_bank: False
  memory_bank_size: 0
  memory_bank_warmup_steps: 0

  share_tower_params: True

  bidirectional_loss: True
  mention_to_entity_weight: 0.8
  entity_to_mention_weight: 0.7

  num_experts: 4
  top_k_experts: 2
  expert_hidden_dim: 256

  load_balance_weight: 0.05
  channel_balance_weight: 0.2
  diversity_weight: 0.1
  expert_dropout_rate: 0.5

trainer:
  accelerator: 'gpu'
  devices: 1
  max_epochs: 30
  num_sanity_val_steps: 0
  check_val_every_n_epoch: 1
  log_every_n_steps: 50
  gradient_clip_val: 1.0
  accumulate_grad_batches: 1
